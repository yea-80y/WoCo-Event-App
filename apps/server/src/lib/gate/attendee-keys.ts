/** MOVED into `store.ts`, beside the bindings it reads. This shim exists only
 *  because the file could not be removed in the same pass; delete both. */
export { toAttendeeKeyRows, type AttendeeKeyRow } from "./store.js";
