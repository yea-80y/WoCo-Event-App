/** MOVED into `issue.ts` — one module per rail. This shim exists only because
 *  the file could not be removed in the same pass; delete it and this line. */
export { loadBadgeManifest, type BadgeManifestResult } from "./issue.js";
