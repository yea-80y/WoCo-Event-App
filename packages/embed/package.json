{
  "name": "@woco/embed",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.count.config.ts",
    "check": "tsc --noEmit",
    "dev": "vite build --watch",
    "test": "node --import tsx --test test/*.test.ts",
    "build:count": "vite build --config vite.count.config.ts"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "@woco/shared": "*"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^6.0.0",
    "tsx": "^4.19.0"
  }
}
