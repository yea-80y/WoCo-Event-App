/**
 * Where an organiser is told to go when the platform has switched something off
 * on their account and a person has to look at it (#619's sending stop).
 *
 * One constant so the copy, the server message and the docs cannot disagree.
 * The mailbox must exist and be read before launch.
 */
export const SUPPORT_EMAIL = "support@woco-net.com";
